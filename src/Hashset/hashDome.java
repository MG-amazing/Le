package Hashset;import java.util.Scanner;public class hashDome {    //特点无序存储，无索引    //JDK8之前 数组+链表    //JDK8之后 数组+链表+红黑树    //哈希值是JDK根据对象的地址，按照某种规则算出来的int 类型的数值    // hashcode拿到哈希值    //同一个对象的hashcode是相同的    public static void main(String[] args) {        Scanner sc=new Scanner(System.in);        System.out.println(sc.hashCode());    }    //hashset1.7 版本是基于数组和链表来实现的    //hashset1.8 版本开始基于数组链表+红黑树}//总结/** * 如果希望元素可以重复，又有索引，索引查询要快 * ArrayList 集合，基于数组（用的最多） *//** * 若元素可以重复，又有索引 * Linklist集合，基于双链表 *//** * 增删改查都很快，但是元素不重复、无序、无索引。 * 用HashSet集合，基于哈希表的。 *//** * 如果希望增删改查都很快，但是元素不重复，基于哈希表和双链表。 * LinkHashSet集合，基于哈希表和双链表。 *//** * 如果要对对象进行排序。 * 用TreeSet集合，基于红黑树。后续也可以用List集合实现排序。 *///可变参数/** * 可变参数用在形式中可以接受多个数据。 * 可变参数的格式：数据类型。。。参数名称 * 可变参数的作用，方便。可以不传参数，可以传输1个或多个，也可以传输一个数组 */